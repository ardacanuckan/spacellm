[project]
name = "spacellm-docs"
version = "0.0.0"
description = "SpaceLLM documentation site (MkDocs Material)."
requires-python = ">=3.11"
dependencies = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "pymdown-extensions>=10.7",
    "mkdocs-section-index>=0.3",
]

[tool.uv]
package = false
